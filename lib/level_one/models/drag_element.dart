class DragElement {
  final int titleNum;
  final String description;
  bool isDropped;

  DragElement(
      {required this.titleNum,
      required this.description,
      required this.isDropped});
}
